// Consider the animal class that has only one method, walk(). next, we want to create a derived class called bird from animal class that has a fly() method.finally, we can create a bird object that can call both fly() and walk(). you must add a sing method to the bird class, then modify the main method accordingly so as to get the desired output.

Learning outcomes
1. to use Knowledge of the concept of inheritance
2. using the concept of inheritance to create a derived class from a base class and use the derived class to create an object
3. Understanding the use of extends keyword in java to create a derived class from a base class