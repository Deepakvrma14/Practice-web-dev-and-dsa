//postfix expression+infix expression

// 1. take empty stack
// if ch == operand, print ch
// if ch == operator, push ch in between
// if ch == (,{} push it to stack
// if ch == ),} pop all the elements from stack and print it

// infix to postfix

#include<stdio.h>
#include<stdlib.h>
#include<string.h>

int main(){
    
}
